{
  "name": "@react-ssr/web",
  "version": "1.0.0",
  "private": true,
  "main": "./lib/index.js",
  "scripts": {
    "build": "rm -rf dist && rm -rf lib && webpack --mode production",
    "dev": "rm -rf dist && rm -rf lib && webpack --mode production -w",
    "deploy": "yarn build"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^5.0.5"
  }
}
